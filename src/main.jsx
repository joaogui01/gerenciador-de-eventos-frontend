import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import GerenciadorEventosApp from './GerenciadorEventosApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GerenciadorEventosApp />
  </React.StrictMode>
);